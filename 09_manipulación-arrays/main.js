const letters = ['a', 'b', 'c'];
letters.forEach(element => console.log(element));


const letters2 = ['a', 'b', 'c'];
for (let index = 0; index < letters2.length; index++) {
    const element = letters2[index];
    console.log(element)
}

//hacen lo mismo, 'recorren los elementos del arreglo y los imprimen en consola' 