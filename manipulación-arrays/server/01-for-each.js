const letters = ['a','b','c','d','e'];

// primero veremos una iteración normal de este arreglo llamado letras.

for (let i = 0;i < array.letters; i++) {
    const elemento = array[i];
    console.log(elemento);
}